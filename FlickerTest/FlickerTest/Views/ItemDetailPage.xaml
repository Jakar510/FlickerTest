<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="FlickerTest.Views.ItemDetailPage"
             Title="{Binding Title}"
             BackgroundColor="{DynamicResource ContentPageBackGroundColor}">

	<StackLayout Spacing="20" Padding="15" BackgroundColor="{DynamicResource ContentPageBackGroundColor}">
		<Label Text="Text:" FontSize="Medium"  BackgroundColor="{DynamicResource ContentPageBackGroundColor}"/>
		<Label Text="{Binding Item.Text}" d:Text="Item name" FontSize="Small" BackgroundColor="{DynamicResource ContentPageBackGroundColor}"/>
		<Label Text="Description:" FontSize="Medium"  BackgroundColor="{DynamicResource ContentPageBackGroundColor}"/>
		<Label Text="{Binding Item.Description}" d:Text="Item description" FontSize="Small" BackgroundColor="{DynamicResource ContentPageBackGroundColor}"/>
    </StackLayout>
    
</ContentPage>